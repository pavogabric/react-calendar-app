import { TimeRangeOptions } from '../enums';

const timeRangeOptions = [
    { label: 'Day', value: TimeRangeOptions.Day },
    { label: 'Week', value: TimeRangeOptions.Week },
    { label: 'Month', value: TimeRangeOptions.Month },
];

export default timeRangeOptions;
